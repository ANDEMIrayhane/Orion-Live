# Neon PostgreSQL Database Connection String
# Example: postgresql://<user>:<password>@<neon-host>/<db>?sslmode=require
DATABASE_URL=

# (Optional) Direct connection URL if required for Prisma migrations
# DIRECT_URL=
